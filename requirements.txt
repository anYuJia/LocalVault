# 网络请求
requests==2.28.1
urllib3==1.26.12
aiohttp>=3.8.0
aiofiles>=0.8.0
PyExecJS>=1.5.1

# 日期时间处理
python-dateutil>=2.8.2

# 进度条显示，增强下载器必需
tqdm>=4.62.3

# 性能优化依赖（增强下载器）
psutil>=5.8.0  # 系统资源监控（可选）

# Web界面依赖
Flask>=2.0.0  # Web框架
Flask-SocketIO>=5.0.0  # WebSocket支持
python-socketio>=5.0.0  # SocketIO客户端
gevent>=23.0.0  # 异步WebSocket支持
gevent-websocket>=0.10.1  # WebSocket协议支持
websocket-client>=1.8.0  # 抖音 IM 接收通道

# 浏览器 Cookie 读取
browser-cookie3>=0.19.1

# 更新包签名校验
PyNaCl>=1.5.0
cryptography>=42.0.0

# 原生窗口
pywebview>=5.0
